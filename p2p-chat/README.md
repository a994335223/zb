# P2P 视频聊天应用

基于 WebRTC 的点对点视频聊天应用，支持群聊和文字消息。

## 🚀 快速开始

### 1. 启动后端服务器

```bash
cd server
npm run dev
```

服务器将在 `http://localhost:3001` 启动

### 2. 启动前端

```bash
cd client
npm run dev
```

前端将在 `http://localhost:5173` 启动

### 3. 访问应用

- 本地访问: http://localhost:5173
- 局域网访问: http://你的IP:5173 (可从其他设备访问)

## 📱 手机访问

1. 确保手机和电脑在同一局域网
2. 查看电脑IP: `ipconfig` (Windows) 或 `ifconfig` (Mac/Linux)
3. 手机浏览器访问: `http://电脑IP:5173`

## 🔧 技术栈

### 前端
- Vue 3 + TypeScript
- Pinia 状态管理
- Socket.io-client
- simple-peer (WebRTC)
- UnoCSS

### 后端
- Node.js + Express
- Socket.io
- TypeScript

## 🌐 生产部署

1. 修改 `client/.env.production` 配置服务器地址
2. 自建 coturn 服务器 (STUN/TURN)
3. 配置 HTTPS (WebRTC 需要)

## 📦 目录结构

```
p2p-chat/
├── client/          # 前端 Vue 项目
│   ├── src/
│   │   ├── components/   # 组件
│   │   ├── composables/  # 组合式函数
│   │   ├── stores/       # Pinia Store
│   │   ├── views/        # 页面
│   │   ├── config/       # 配置
│   │   └── types/        # 类型定义
│   └── ...
├── server/          # 后端 Node.js 项目
│   ├── src/
│   │   └── index.ts     # 信令服务器
│   └── ...
└── README.md
```

